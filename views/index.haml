%html
  %head
    :css
      body {
        font-family: Helvetica;
        line-height: 30px;
        margin: 20px;
      }

      a {
        color: #333;
      }

      a:visited {
        color: #999;
      }
  %body
    - @shows.each do |show|
      %a{ href: "/show?secret=1&show=#{show[:href]}" } #{show[:name]}
      %br
